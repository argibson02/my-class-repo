//WRITE YOUR CODE BELOW

var customerOrder = {
    drinkName: "Frappachino",
    sugarCount: 99,
    isReady: true,
}
/*console.log(customerOrder.drinkName + " with " + customerOrder.sugarCount + "g of sugar is")*/

function isReady() {
    if (customerOrder.isReady = true) {
        console.log(customerOrder.drinkName + " with " + customerOrder.sugarCount + "g of sugar is ready for pick up")
    }  
    else {
        console.log("Still in order queue")
    }  
}
    